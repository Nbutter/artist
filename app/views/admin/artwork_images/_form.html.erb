<%= form_for(@artwork_image) do |f| %>
  <% if @artwork_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artwork_image.errors.count, "error") %> prohibited this artwork image from being saved:</h2>

      <ul>
      <% @artwork_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :shot_type %><br>
    <%= f.text_field :shot_type %>
  </div>
  <div class="field">
    <%= f.label :artwork_id %><br>
    <%= f.number_field :artwork_id %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
